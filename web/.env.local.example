# Next.js dev server proxy target (used by next.config.mjs rewrites)
BACKEND_ORIGIN=http://127.0.0.1:8001

# Optional: direct API path override for fetch clients.
# Keep default (/api/v1) when using proxy rewrites.
# NEXT_PUBLIC_API_BASE_PATH=/api/v1
